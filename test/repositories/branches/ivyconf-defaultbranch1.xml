<ivyconf>
    <conf defaultResolver="default" defaultBranch="branch1" />
    <resolvers>
		<filesystem name="default">
			<ivy pattern="${ivy.conf.dir}/[organisation]/[module]/[branch]/[revision]/[artifact].[ext]" />
			<artifact pattern="${ivy.conf.dir}/[organisation]/[module]/[branch]/[revision]/[artifact].[ext]" />
		</filesystem>
    </resolvers>
</ivyconf>
